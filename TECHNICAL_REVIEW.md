# 金融 MoE Transformer — 技术方案全面审查

> 审查时间：2026-05-18 | 数据集：15.4M 数据点 / 719K 标签 / 2659 窗口

---

## 一、发现的关键问题

### 🔴 问题 1：标签计算错误（致命）

**现象**：label std=1.88，mean=-0.002，范围 [-8.8, +8.7]

**原因**：`prepare_data.py` 中找标签价格序列时使用 `str.contains("close|price|index|沪深")`，匹配到了**所有个股的 close 列**（180 只股票 × 每日收盘价），而非沪深300指数。

**后果**：模型学习的是"个股价格变化"而非"沪深300超额收益"，任务目标完全偏离设计意图。

**修复**：标签必须使用 CSI 300 指数日收益率：
```
label = ln(CSI300_close_{t+21} / CSI300_close_t) - rf_daily * 21
```

### 🔴 问题 2：序列严重截断（致命）

**现象**：所有 464 个样本的 seq_len 恰好 = 2048（max_seq_len）

**原因**：每个窗口包含 ~180 只股票 × ~252 天 × 11 变量 ≈ **50 万+ tokens**，全部被截断到 2048。信息丢失 > 99%。

**修复**：
- 减少股票数量（50 只代表股）
- 降低时间粒度（周频聚合）
- 或提高 max_seq_len 到 8192（线性注意力 O(L) 可以处理）

### 🟡 问题 3：time_since_update 恒为 0

**现象**：P50/P75/P99 全部为 0

**原因**：Z-score 标准化后重新计算 time_since_update 时，groupby 操作重置了差异。

**修复**：在 Z-score 之前计算 time_since_update，计算完成后保留原始值。

### 🟡 问题 4：sentiment 数据日期异常

**现象**：49 条数据全部显示 "1970-01-01"

**原因**：北向资金 CSV 的日期列未被正确解析（trade_date 格式可能是 20250516 而非标准日期）。

**修复**：解析 `trade_date` 为 `pd.to_datetime(..., format='%Y%m%d')`

### 🟡 问题 5：financial 数据未被利用

**现象**：利润表有 102 个变量、3.9M 行，但只有 75 个唯一日期（季度频率）

**原因**：原始数据中很多字段是非数值的（分类、字符串），且被错误映射。

**修复**：只保留数值型财务指标（净利润、营收、ROE、EPS 等 ~20 个核心指标），过滤掉元数据列。

---

## 二、架构评估

### 当前架构

| 组件 | 配置 | 评估 |
|------|------|------|
| 嵌入维度 | d_model=128 | ✅ 合理，金融序列信息密度适中 |
| 注意力 | cosFormer 线性注意力 | ✅ 适合长序列，O(L·d²) 复杂度 |
| 层数 | 2 层 | ⚠️ 偏少，可增至 4 层 |
| 头数 | 4 头 × 32 dim | ✅ 合理 |
| FFN | 256 | ⚠️ 偏小，可增至 512 |
| MoE 专家 | 4 个，128→32→1 | ⚠️ 专家容量偏小 |
| 总参数量 | 365K | ✅ 远低于 2M 上限 |

### 改进建议

#### A. 增加模型容量（目标 ~800K 参数）

```python
D_MODEL = 128        # 保持不变
NHEAD = 4            # 保持不变
NUM_LAYERS = 4       # 2 → 4 层
DIM_FEEDFORWARD = 512 # 256 → 512
NUM_EXPERTS = 6      # 4 → 6
EXPERT_HIDDEN = 64   # 32 → 64
```

预计参数量：~800K，仍远低于 2M。显存增加有限（< 0.5 GB）。

#### B. 引入分组嵌入

当前所有变量共享同一个嵌入空间。改进：**按 source 分组**，不同来源的变量使用独立的嵌入表：

- market 嵌入（11 个变量）
- financial 嵌入（~20 个核心指标）
- macro 嵌入（~12 个变量）
- 共 3 个嵌入表，维度占比可学习或固定

#### C. 注意力改进

cosFormer 的余弦权重基于绝对位置。更适合金融场景的是 **ALiBi 风格的时间衰减偏置**：
- 较远的 token 注意力自然衰减
- 不需要额外的位置编码开销

#### D. 池化策略改进

当前使用 mean pooling。更好的方案：

- **Attention Pooling**：学习一个 query 向量，对所有 token 做加权平均
- **Last-N Pooling**：仅取最后 N 个 token（最近的数据点更重要）
- **Hierarchical Pooling**：先按 source 分组池化，再拼接

---

## 三、训练流程评估

### 当前配置

| 参数 | 值 | 评估 |
|------|-----|------|
| Batch size | 4 × 4 累积 = 16 | ✅ |
| LR | 3e-4 | ✅ |
| LR schedule | Cosine + warmup 500 | ✅ |
| 损失 | MSE + 0.01×负载均衡 | ⚠️ 可改进 |
| 早停 | patience=5 | ✅ |
| 设备 | CPU (i7-8750H) | ⚠️ 需 GPU |
| 速度 | ~16 min/epoch | ⚠️ 偏慢 |

### 改进建议

#### A. 损失函数改进

当前只有 MSE + 负载均衡。建议增加：

1. **秩相关系数损失（Spearman Loss）**：对金融预测更重要的排序能力
   ```python
   loss = MSE + 0.5 * (1 - spearman_correlation(pred, label))
   ```

2. **方向准确率损失**：预测方向正确的额外奖励
   ```python
   direction_loss = BCEWithLogits(pred_sign, label_sign)
   ```

3. **概念正则化**（已有框架，未启用）：
   利用变量描述之间的语义相似度矩阵，约束注意力模式。

#### B. 数据增强

1. **时间窗口抖动**：每个 epoch 随机变化窗口长度（200-300 天）
2. **Token Dropout**：已实现 5% 遮蔽，可增至 10%
3. **变量置换**：随机交换同 source 内某些变量的顺序

#### C. 验证策略

当前按时间顺序 split（70/15/15）。金融时序建议：

- **Purged Walk-Forward**：避免训练集和验证集的信息泄露
- **跨年份验证**：每年作为一个 fold，测试模型在不同市场环境下的稳健性

---

## 四、数据诊断

### 数据量分布

| source | 行数 | 变量 | 日期数 | 问题 |
|--------|------|------|--------|------|
| market | 11.3M | 11 | 5,424 | 每个窗口截断严重 |
| financial | 4.0M | 102 | 75 | 非数值列多，频率低 |
| macro | 12K | 11 | 913 | 变量名含中文特殊字符 |
| alternative | 100K | 9 | 6,987 | 混杂碳市场+futures |
| sentiment | 294 | 6 | 49 | 日期解析错误 |

### 建议的数据切片

**训练用数据应缩减到**：

| source | 保留变量 | 频率 | 说明 |
|--------|----------|------|------|
| market | 5 (close/volume/peTTM/pbMRQ/turn) | 日 → 周 | 取周均值，减少 80% token |
| financial | 12 (ROE/EPS/净利润/营收/毛利率/负债率/现金流等) | 季 | 仅核心指标 |
| macro | 8 (CPI/PPI/PMI/M2/GDP/LPR/shibor/国债) | 月 | — |
| sentiment | 4 (融资余额/北向净买入/南向净买入) | 日 | 修复日期 |
| text_embed | 128 | 周 | 待实现 |
| llm_state | 4 | 月 | 待实现 |

**优化后每个窗口约 2000-4000 tokens**，不再截断。

---

## 五、改进优先级

### P0（立即修复，阻塞训练）

| 问题 | 修复难度 | 预计时间 |
|------|----------|----------|
| 标签计算改为 CSI 300 指数 | 简单 | 10 分钟 |
| 修复 time_since_update | 简单 | 5 分钟 |
| 修复 sentiment 日期 | 简单 | 5 分钟 |

### P1（显著改善效果）

| 改进 | 难度 | 预计时间 |
|------|------|----------|
| 减少股票数量到 50 只 | 简单 | 5 分钟 |
| 过滤 financial 非数值列 | 中等 | 15 分钟 |
| 增加模型层数到 4 层 | 简单 | 2 分钟 |
| 增加 FFN 到 512 | 简单 | 2 分钟 |

### P2（进一步提升）

| 改进 | 难度 | 预计时间 |
|------|------|----------|
| Attention Pooling 替代 mean | 中等 | 20 分钟 |
| Spearman 排序损失 | 中等 | 15 分钟 |
| 周频聚合减少 token 数 | 中等 | 20 分钟 |
| 跨年份交叉验证 | 中等 | 30 分钟 |

### P3（增强功能）

| 改进 | 难度 | 预计时间 |
|------|------|----------|
| 接入 yfinance 获取美股/港股 | 中等 | 30 分钟 |
| BGE 文本编码（新闻标题） | 中等 | 1 小时 |
| Qwen 市场状态生成 | 中等 | 1 小时 |
| 概念正则化 | 困难 | 2 小时 |

---

## 六、修复后的预期

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| 有效 tokens/窗口 | 2048（截断） | 2000-4000（无截断） |
| 模型参数量 | 365K | ~800K |
| 训练速度 (CPU) | 16 min/epoch | ~10 min/epoch |
| 标签含义 | 个股价格变化（错误） | 沪深300超额收益（正确） |
| 可用窗口数 | 2659 | ~2000+ |
| 预期 IC | 不可用 | 0.03-0.08（合理范围） |

---

## 七、总结

**当前状态**：基础架构正确，但存在标签计算错误和数据质量问题，**不能直接训练**。

**需要修复后才能开始训练**。预计修复时间：30-60 分钟（P0+P1）。

**修复后，CPU 训练约 6-8 小时，GPU 约 2-3 小时**。
