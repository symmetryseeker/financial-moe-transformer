# Core
torch>=2.1.0
numpy>=1.24.0,<2.0
pandas>=2.0.0
pyarrow>=12.0.0
scikit-learn>=1.3.0

# Data acquisition
tushare>=1.4.0
akshare>=1.12.0

# Text encoding (offline)
sentence-transformers>=2.3.0

# LLM for synthetic features (offline)
transformers>=4.36.0
bitsandbytes>=0.41.0
accelerate>=0.25.0

# Training (optional — native loop works fine without)
tensorboard>=2.14.0

# Utilities
tqdm>=4.65.0
openpyxl>=3.1.0
